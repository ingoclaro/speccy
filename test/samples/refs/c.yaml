post:
  summary: c
  parameters:
    - $ref: './parameters.yaml#/foo'
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/myobject'
  responses:
    '405':
      description: Invalid input
